import 'package:flutter/material.dart';

class ShelterSignupScreen extends StatelessWidget {
  const ShelterSignupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
